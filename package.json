{
  "scripts": {
    "postinstall": "bower install --quiet",
    "start": "./script/start && testem -f test/testem.json && ./script/stop",
    "stat": "cloc index.js --quiet --by-file && cloc test/ --quiet --by-file --exclude-dir=support",
    "test": "./script/start && testem -f test/testem.json ci && ./script/stop"
  },
  "dependencies": {
    "socket-server": "git+ssh://git@github.com/ask11/socket-server.git#0.1.0"
  },
  "devDependencies": {
    "testem": ">= 0.4",
    "bower": ">= 1.0"
  }
}
